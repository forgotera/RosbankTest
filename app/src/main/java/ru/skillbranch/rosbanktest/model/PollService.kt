package ru.skillbranch.rosbanktest.model

import android.app.IntentService
import android.content.Intent

class PollService(name: String?) : IntentService(name) {
    override fun onHandleIntent(p0: Intent?) {
    }
}